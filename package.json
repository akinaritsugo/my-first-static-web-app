{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "debug": "swa start --app-location ./src --api-location ./api",
    "playwright_test": "playwright test"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.0.4",
    "@playwright/test": "^1.22.2"
  }
}
